<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ngtesting.platform.dao.WelcomeDao" >

    <select id="test" resultType="SysNums">
        SELECT o.*

        FROM SysNumsDefine o

        ORDER BY o.`key` LIMIT 5
    </select>

</mapper>
