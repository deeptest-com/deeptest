<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ngtesting.platform.dao.OrgGroupDao" >

    <select id="query" resultType="TstOrgGroup">
        SELECT * FROM TstOrgGroup grp

        WHERE TRUE
        <if test="keywords != null and keywords != ''">
            AND usr.name LIKE CONCAT('%','${keywords}','%' )
        </if>
        <if test="disabled != null and disabled != ''">
            AND grp.disabled = #{disabled}
        </if>

        AND grp.orgId = #{orgId}
        AND grp.deleted != TRUE
        AND grp.disabled != TRUE
        ORDER BY grp.id ASC
    </select>

    <select id="search" resultType="TstOrgGroup">
        SELECT * FROM TstOrgGroup grp

        WHERE grp.orgId = #{orgId}
        <if test="keywords != null and keywords != ''">
            AND grp.name LIKE CONCAT('%','${keywords}','%' )
        </if>
        <if test="exceptIds != null and exceptIds != ''">
            AND grp.id NOT IN #{exceptIds}
        </if>

        AND grp.deleted != TRUE
        AND grp.disabled != TRUE
        ORDER BY grp.id ASC
    </select>

    <select id="get" parameterType="int" resultType="TstOrgGroup">
        SELECT * FROM TstOrgGroup grp

        WHERE grp.id = #{id}
    </select>

    <insert id="save" parameterType="TstOrgGroup" useGeneratedKeys="true" keyProperty="id">
        insert into TstOrgGroup
          (name,orgId,deleted,disabled,createTime,version)
        values
          (#{name},#{orgId},FALSE,FALSE,NOW(),0)
    </insert>

    <update id="update" parameterType="TstOrgGroup">
        UPDATE TstOrgGroup
        SET name=#{name},orgId=#{orgId},
        disabled=#{disabled},
        updateTime=NOW()

        WHERE id=#{id}
    </update>

</mapper>
