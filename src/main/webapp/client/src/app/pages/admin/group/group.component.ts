import {Component} from '@angular/core';

import { CONSTANT } from '../../../utils/constant';
import { Utils } from '../../../utils/utils';

@Component({
  selector: 'group',
  styles: [require('./group.scss')],
  template: require('./group.html')
})
export class Group {

  constructor() {
  }

  ngOnInit() {
  }

}
