<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ngtesting.platform.dao.IsuTypeSolutionDao" >

    <select id="list" resultType="IsuTypeSolution">
        SELECT * FROM IsuTypeSolution o
        WHERE o.orgId=#{orgId}

        AND o.disabled!=TRUE AND o.deleted!=TRUE
        ORDER BY ordr ASC
    </select>

    <select id="get" resultType="IsuTypeSolution">
        SELECT * FROM IsuTypeSolution o

        WHERE o.id = #{id}
        <if test="orgId != null">
            AND o.orgId=#{orgId}
        </if>
    </select>

    <insert id="save" parameterType="IsuTypeSolution" useGeneratedKeys="true" keyProperty="id">
        insert into IsuTypeSolution
        (label,value,descr,orgId,deleted,disabled,createTime)
        values
        (#{label},#{value},#{descr},#{orgId},FALSE,FALSE,NOW())
    </insert>

    <update id="update" parameterType="IsuTypeSolution">
        UPDATE IsuTypeSolution
        SET label=#{label},value=#{value},descr=#{descr},
        disabled=#{disabled},
        updateTime=NOW()

        WHERE id=#{id} AND orgId=#{orgId}
    </update>

    <delete id="delete">
        UPDATE IsuTypeSolution
        SET disabled=TRUE, updateTime=NOW()
        WHERE id=#{id} AND orgId=#{orgId}
    </delete>

</mapper>
