import {ProcessorCategory} from "@/utils/enum";

/*
export const isFolder = (entityCategory) => {
    return entityCategory === ProcessorCategory.ProcessorRoot
        || entityCategory === ProcessorCategory.ProcessorThread
        || entityCategory === ProcessorCategory.ProcessorGroup
        || entityCategory === ProcessorCategory.ProcessorLoop
        || entityCategory === ProcessorCategory.ProcessorLogic
        || entityCategory === ProcessorCategory.ProcessorData
}*/
