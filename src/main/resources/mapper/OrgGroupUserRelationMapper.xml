<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ngtesting.platform.dao.OrgGroupUserRelationDao" >

    <select id="query" resultType="TstOrgGroupUserRelation">
        SELECT * FROM TstOrgGroupUserRelation o WHERE o.orgId=#{orgId}
        <if test="groupId != null">
            AND o.orgGroupId = #{groupId}
        </if>
        <if test="userId != null">
            AND o.userId = #{userId}
        </if>
        AND o.deleted != true AND o.disabled != true
        ORDER BY o.id ASC
    </select>

</mapper>
